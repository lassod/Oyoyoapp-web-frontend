"use client";
import React from "react";
import EditEvent from "./EditEvent";

const CreatorEventView = ({ event }: any) => {
  return <EditEvent event={event} />;
};

export default CreatorEventView;
